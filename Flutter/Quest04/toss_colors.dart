import 'package:flutter/material.dart';

class TossColors {
  // Background colors
  static const Color background = Color.fromRGBO(23, 23, 27, 1);

  // Text colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color.fromRGBO(195, 195, 198, 1);
  static const Color textTertiary = Color.fromRGBO(158, 158, 163, 1);

  // Badge colors
  static const Color badgeBackground = Color.fromRGBO(244, 187, 115, 0.2);
  static const Color badgeText = Color.fromRGBO(237, 165, 75, 1);

  // Border colors
  static const Color border = Color.fromRGBO(76, 76, 87, 1);
}
